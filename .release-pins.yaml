# v1.0.0-alpha ships the registration-string path used by commands/*/configure.md:
# a rune-mcp whose configure tool accepts `registration_string` (3-stage bootstrap),
# with the matching runed (centroid routing / SetCentroids).
rune_mcp_version: v1.0.0-alpha
runed_version: v1.0.0-alpha
